<!DOCTYPE html>
<html>
  <head>
    <title>RPG</title>
    <p id="notice"><%= notice %></p>
    <meta name="format-detection" content="telephone=no">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta charset="utf-8">
  </head>
  <body > 
    <%= render 'layouts/header' %>
    <%= yield %>
    <div class="page-loader">
      <div class="page-loader-body">
        <div class="cssload-jumping"><span></span><span></span><span></span><span></span><span></span></div>
      </div>
    </div>
    <div class="page text-center">
      <section>
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-sm-10 col-lg-8">
              <hr class="divider-sm divider-success">
              <p id="notice"><%= notice %></p>
              <div class = "div-border-back-2">
                <h1><%= @historium.nome %></h1>
                <table>
                  <tbody>
                    <% @Opcao.each do |opcoes| %>
                        <%= link_to opcoes.texto, @nexthistoria = Historium.where("id = ?", opcoes.filhahistoria_id).take, class: "btn btn-white-outline btn-lg btn-aqil btn-aqil--mod-1" %>
                        <br>
                    <% end %>  
                    <br>
                    <%= @historium.texto %>
                  </tbody>
                </table>
                <br>
              </div>
              <br>
              <%= link_to 'Voltar ao Inicio', historia_path , class: "btn btn-white-outline btn-lg btn-aqil btn-aqil--mod-1", margin: "auto" %>
            </div>
          </div>
        </div>
      </section>
    </div>
  </body>
</html>
